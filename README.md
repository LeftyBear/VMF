# VMF

VMF is the repository for the VMF architecture, Build.xlam, and applications
that use VMF.

Build.xlam v1.1 and VMF v1.0 are official releases. Frozen specifications are
not modified in place; future improvements are recorded as candidate documents.

---

## Architecture

```text
Presentation
    -> Application
        -> Domain
            -> Common

Infrastructure
    -> Common
```

Dependencies are one-way. Circular dependencies are prohibited.

---

## Directory Structure

```text
src/            Build source and generated VMF source workspace
tests/          Unit and integration tests
specs/          Official Build and VMF specifications
candidates/     Future candidate changes
docs/           Development, build, and release documentation
templates/      Build generation templates
tools/          Build, test, and audit tools
applications/   Applications that use VMF
dist/           Generated distribution artifacts only
```

The SchoolTimetable application workspace is under
`applications/SchoolTimetable/`.

Build generates source files from generation definitions such as manifests,
templates, and blueprints, then builds add-ins from those generated sources.
`src/VMF` is the working folder for generated VMF source files. Files generated
there are intermediate artifacts and can be regenerated by Build, so they are
not managed by Git. Do not edit generated files in `src/VMF` directly; update
the generation definitions instead.

---

## VMF Studio v2.0 Publisher PoC v0.1

`VMF.Publisher.sln` contains the first Publisher PoC. It reads UTF-8 Markdown,
converts headings, paragraphs, and unordered-list items into a neutral document
model, compiles Google Docs operations, creates a native document through the
Google Drive REST API, and applies content through the Google Docs REST API.

The four projects are:

- `src/Publisher/` — Application, Domain, and Infrastructure implementation
- `src/Publisher.Cli/` — `vmf-publisher` composition root and CLI
- `tests/unit/Publisher/` — parser, compiler, and request-mapper tests
- `tests/integration/Publisher/` — file-to-publication-pipeline integration test

Build and test:

```powershell
dotnet restore VMF.Publisher.sln
dotnet build VMF.Publisher.sln --no-restore
dotnet test VMF.Publisher.sln --no-build
dotnet run --project src/Publisher.Cli -- --help
dotnet run --project src/Publisher.Cli -- publish path/to/document.md
```

The PoC authenticates with a Google service-account JSON file. Set
`Google:CredentialsPath` in a local settings copy or use the
`VMF_PUBLISHER_CREDENTIALS_PATH` environment variable. `Google:FolderId` can
optionally select a destination folder. The `TokenStorePath` setting is reserved
for a future installed-application OAuth flow and is not used in v0.1.

Credential JSON, token stores, and local appsettings files must remain outside
version control. The checked-in `appsettings.json` contains no secrets.

Not implemented in v0.1: tables, images, footnotes, embedded HTML, ordered
lists, nested lists, inline emphasis/link styling, and installed-application
OAuth/token-store persistence.

The current official VMF Studio release artifact is:

```text
dist/release/VMFStudio_1.1.0/VMFStudio_1.1.0.xlam
```

---

## Release Reports

- `docs/releases/VMF_v1.0_ReleaseReport.md`
- `docs/releases/Build_v1.0.1_ReleaseReport.md`
- `docs/releases/Build_v1.0.2_ReleaseReport.md`
- `docs/releases/Build_v1.1_ReleaseReport.md`
- `RELEASE_NOTES_v1.1.md`

---

## VMF Studio v1.1 Documents

- `USER_GUIDE.md`
- `PUBLIC_API.md`
- `ERROR_CODES.md`
- `specs/build/MANIFEST_SPEC.md`
- `specs/build/TEMPLATE_SPEC.md`

---

## Development Rules

- Do not modify frozen VMF v1.0 specifications in place.
- Do not modify frozen Build v1.0.x specifications in place.
- Record future improvements under `candidates/`.
- Keep specifications, implementation, tests, tools, documentation, and
  generated artifacts separated by directory responsibility.
- Treat GitHub as the single source of truth.

---

## License

See [LICENSE](LICENSE).
