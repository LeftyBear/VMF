# VMF Studio Publisher PoC

This paragraph verifies that UTF-8 Markdown text is published to Google Docs.

## Live verification checklist

- Unordered level one uses a disc.
  - Unordered level two uses a circle.
    - Unordered level three uses a square.
      - Unordered level four remains nested.
1. Ordered level one uses a decimal marker.
  1. Ordered level two uses an alpha marker.
    1. Ordered level three uses a Roman marker.
- A mixed list starts with an unordered item.
  1. Its second level is ordered.
    - Its third level returns to unordered.
      1. Its fourth level returns to ordered.

This paragraph follows the list and verifies that its position is preserved.
