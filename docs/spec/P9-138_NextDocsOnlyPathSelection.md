# P9-138 — Next Docs-Only Path Selection

## Decision

P9-138 is **COMPLETE / docs-only next-path selection**.

Selected docs-only path: **Static risk-control matrix drafting**.

Technical execution remains **NO-GO / SAFE-STOP**. No technical execution
candidate, command, or operation is selected or authorized. Avast detection
remains unresolved, and the P9-94 allowance remains not reusable.

## Selection Rationale

The static risk-control matrix is the smallest next documentation step because
it can map the existing P9 safety boundaries, required evidence, prohibitions,
and stop conditions without defining or running a command. It provides a
governance basis for any later consideration of allowlist hardening or a future
technical GO / NO-GO template.

Exact command allowlist hardening, future technical GO / NO-GO template
drafting, and non-executable artifact inventory planning remain unselected.

## Future Drafting Boundary

A later, separately instructed P9-139 may draft a static risk-control matrix
limited to existing authoritative P9 records. The matrix may organize:

- risk or boundary;
- existing control or prohibition;
- evidence required before reconsideration;
- safe-stop trigger; and
- authorization dependency.

P9-138 does not define an executable command, permit source or test changes,
accept new security evidence, resolve the Avast detection, or grant continuation
or technical execution authorization.

## Preserved Safety Boundary

P9-130 and P9-135 MUST NOT be rerun. No parser, script, PowerShell script,
Excel, workbook, process, test, build, package, `dist`, release, publication,
tag, external-service, flagged-executable, or Avast operation or workaround is
authorized.

The completed P9-135 authorization is exhausted. The P9-94 allowance does not
carry forward and cannot be reused.

## Next Step

**P9-139 — Static Risk-Control Matrix Drafting**

P9-139 requires a separate docs-only instruction. It inherits no technical
execution authorization from P9-138.
