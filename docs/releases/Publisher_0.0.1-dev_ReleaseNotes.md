# Publisher 0.0.1-dev Release Notes

Date: 2026-08-09

## Scope

These notes record the next Publisher release identity only:

- Version: `0.0.1-dev`
- Tag: `vmf-publisher-v0.0.1-dev`
- Runtime: `win-x64`
- Configuration: `Release`
- Package type: framework-dependent (`selfContained=false`)
- Package:
  `dist\release\Publisher\vmf-publisher-0.0.1-dev-win-x64.zip`

The package has been generated and verified for this identity. No tag has been
created. No GitHub Release has been created. No publication has been
performed.

## Identity Status

`0.0.1-dev` is adopted as the next release identity because the prior
`0.0.0-dev` identity is already used as a tag, GitHub Release, and published
asset identity. The existing `vmf-publisher-v0.0.0-dev` tag, GitHub Release,
and asset remain immutable historical records.

The target commit is fixed:

- target commit: `f08eef306ba82e3ea7f031ef652666178f2f0acf`.

The verified package artifact identity is fixed:

- package path: `dist\release\Publisher\vmf-publisher-0.0.1-dev-win-x64.zip`;
- package size: 983404 bytes;
- package SHA-256:
  `73582c24e4c3bf279aeb8fd2044b84a30a3d621eac623188dcfa4406ac32bcc6`.

Package generation and package verification are recorded as `PASS`.
Package manifest identity records `version=0.0.1-dev`,
`runtimeIdentifier=win-x64`, `configuration=Release`,
`selfContained=false`, and 14 manifest files. Secret/static package
inspection is recorded as `PASS`.

Final verification, Live E2E 4/4 `PASS`, result review, package generation,
and package verification form the recorded identity chain for this package.
At record time `HEAD == origin/main == f08eef306ba82e3ea7f031ef652666178f2f0acf`
and the working tree was clean before this docs-only update.

## Release Boundary

This release-note file is documentation only. It does not authorize or perform:

- package generation;
- `dist` updates;
- tag creation, deletion, movement, or retargeting;
- GitHub Release creation or update;
- asset replacement;
- publication or release announcement;
- Live E2E;
- Google Docs or Google Drive mutation;
- production code, test, public API, schema, or Frozen specification changes;
- staging, commit, or push.

ADR-0016 traceability is preserved by keeping the next identity separate from
the historical `0.0.0-dev` release and by recording the fixed target commit,
verified package path, size, SHA-256, package verification, final verification,
Live E2E, and result-review chain without implying tag/release execution.

The intended tag is `vmf-publisher-v0.0.1-dev`, but it has not been created.
GitHub Release creation and publication have not been performed. This
docs-only update is not tag creation, release execution authorization, GitHub
Release authorization, publication authorization, staging authorization,
commit authorization, or push authorization.
