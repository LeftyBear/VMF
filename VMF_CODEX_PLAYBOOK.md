# VMF Codex Playbook v1.0

## 1. 目的

本Playbookは、VMFおよびVMF Publisherに対するCodex作業を、以下の方針で統一するための共通運用規約である。

* Codex使用量を抑える
* 設計と実装の責務を分離する
* Frozen仕様を保護する
* 変更範囲を限定する
* テスト可能性と安全性を維持する
* レビュー可能な差分を残す
* Git履歴をCodexに変更させない
* 実装結果の報告形式を統一する

Codexは、本Playbookとタスク固有指示の両方に従うこと。

両者に矛盾がある場合は、より具体的なタスク固有指示を優先し、それ以外は本Playbookに従う。

---

## 2. 基本原則

### 2.1 設計と実装の分離

設計、仕様決定、Phase分割、受け入れ条件の確定は、Codex実行前に完了しているものとする。

Codexは以下を担当する。

* 既存設計に基づく実装
* テスト追加
* ローカル検証
* 変更差分の整理
* 実装結果の報告

Codexは以下を担当しない。

* 新規仕様の決定
* Frozen仕様の再解釈
* 要件の拡張
* 公開APIの再設計
* アーキテクチャ全体の変更
* 対象Phase外の改善

### 2.2 最小変更

変更は、受け入れ条件を満たすために必要な最小範囲に限定する。

禁止事項:

* 関係のないリファクタリング
* 一括整形
* 命名規則の全面変更
* 将来用途を想定した先行実装
* 対象外テストの変更

### 2.3 Frozen仕様の保護

Frozen仕様、公開契約、永続化形式、Canonical形式は変更しない。

改善案は **VMF v1.1 Candidate** として提案し、現行実装へ反映しない。

### 2.4 保守的解釈

仕様が曖昧な場合は次の優先順位で判断する。

1. タスク固有指示
2. Frozen仕様
3. 既存テスト
4. 公開契約
5. 既存実装パターン
6. 本Playbook

判断できない場合は安全停止し、報告する。

---

## 3. Codex使用量を抑える運用

* 1回の依頼は1つの目的に限定する。
* 変更対象を明示する。
* タスクに必要なファイルのみ探索する。
* タスク固有仕様、影響を受ける実装ファイル、Focused Testを優先して確認する。
* 必要な場合を除き、リポジトリ全体の探索を行わない。
* 同一タスク内で既に確認した権威あるファイルを、必要なく繰り返し読み直さない。
* 憶測による調査を避ける。
* 推測による実装を避ける。
* 具体的な必要がない限り、別案探索より既存実装パターンを優先する。
* 広範な検証の前にFocused Verificationを実行する。
* 実装後にまとめてレビューする。
* 実装・レビュー・設計を1回の依頼へ混在させない。
* 必要な報告事項を維持しつつ、最終報告は簡潔にする。

---

## 4. 推奨Codex設定

### 既定

* モデル: GPT-5.6 Sol
* 思考レベル: 低
* 速度: 標準

各タスクは、宣言された範囲に十分な最も低い思考レベルから開始する。

思考レベルの引き上げは、必要な場合に限り、次の順序で行う。

```text
Low -> Medium -> High
```

Highを既定として開始しない。

### Lowを使用する作業

Routine、docs-only、決定論的、または既存パターンに従う作業に使用する。

対象例:

* docs-only変更
* status、backlog、handoff更新
* 小規模または決定論的な実装変更
* Focused Test追加
* 確立済みの実装パターンに従う変更
* 軽微なbug fix
* 権威ある仕様と受け入れ条件が既に明確なタスク

### Mediumへ引き上げる作業

実質的な判断が必要な場合に使用する。

対象例:

* 境界設計または境界解釈
* 複数の権威ある仕様の整合性確認
* GO / NO-GO判断
* 公開契約への影響分析
* 複数componentにまたがる実装
* 非自明なdebugging
* Lowでは安全に解決できない曖昧さ

### Highを使用する作業

真に複雑な作業にのみ使用する。

対象例:

* crash consistency
* 永続化整合性
* retryまたはre-execution safety
* move detection
* 複雑なdiff algorithm
* 複雑なstate transition
* partial-failureまたはconflict handling
* 複数の安全境界が相互作用する作業

原則として最高思考レベル、maximum reasoning、高速モード、多数の独立タスク同時実行は使用しない。

思考レベルの引き上げは、Frozen仕様保護、公開契約保護、アーキテクチャ境界、外部サービス安全性、テスト要件、検証要件、安全停止条件、Git制限を弱めてはならない。

---

## 5. 実装前確認

作業開始前に確認する。

* 現在ブランチ
* Git状態
* 対象Solution
* 関連テスト
* 変更範囲

ユーザーの既存変更を削除・上書きしない。

---

## 6. 実装規約

* 公開契約を変更しない。
* 新規依存関係を追加しない。
* エラーを握りつぶさない。
* 決定論的動作を維持する。
* 再実行安全性を維持する。
* 部分成功を成功として扱わない。

---

## 7. 外部サービス

既定では実更新を行わない。

Google Docs、Google Drive等への更新は明示的に許可された場合のみ実施する。

認証情報、Secrets、Token Storeは変更・出力しない。

---

## 8. テスト規約

実装対象に対するFocused Testを先に実行し、その後に全体テストを実施する。

テストを削除・無効化・緩和して成功させてはならない。

---

## 9. 検証順序

1. Focused Test
2. 全Unit Test
3. 必要なIntegration Test
4. Release Build
5. Format Check
6. Diff Check

---

## 10. Git運用

禁止事項:

* git add
* git commit
* git push
* git pull
* git merge
* git rebase
* git reset
* git stash
* git clean

レビュー可能な未コミット差分を残して停止する。

---

## 11. 文書更新

文書更新はタスク固有指示で要求された場合のみ実施する。

Frozen文書、Canon、VMF仕様、ADR、RFC、CHANGELOG、Release Notesを無断で変更しない。

---

## 12. 完了条件

完了時には以下を満たすこと。

* 必須要件を実装
* 対象外変更なし
* Frozen仕様維持
* 公開契約維持
* テスト追加・更新完了
* Focused Test成功
* 全テスト成功
* Release Build成功
* Format Check成功
* Diff Check成功
* stage・commit・push未実施

未達成項目がある場合は完了と報告しない。

---

## 13. 安全停止条件

以下の場合は停止して報告する。

* Frozen仕様との矛盾
* 公開契約変更が必要
* 永続化形式変更が必要
* 新規依存が必要
* ユーザー変更と競合
* 実データ更新が必要
* 認証情報が必要
* 破壊的Git操作が必要
* テスト緩和が必要
* 設計判断が確定できない

---

## 14. 最終報告形式

### 実装結果

* 対象Phase
* 実装内容
* 追加ファイル
* 変更ファイル
* テスト
* 文書更新

### 設計判断

* 採用した解釈
* 維持した不変条件
* 互換性
* 安全停止条件
* 対象外事項

### 検証結果

* Focused Test
* 全Unit Test
* Integration Test
* Release Build
* Warning数
* Error数
* Format Check
* Diff Check

### Git状態

* Branch
* Stage
* Commit
* Push
* Working Tree

### 残課題

* 未完了項目
* 原因
* 次に必要な対応
